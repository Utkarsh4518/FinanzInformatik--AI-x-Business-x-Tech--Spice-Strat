# Forge variables are the primary deployment mechanism.
# These values are optional and only needed if you switch from the built-in
# heuristic AI provider to the OpenAI-backed provider in a later iteration.

SPECBRIDGE_AI_PROVIDER=heuristic
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
